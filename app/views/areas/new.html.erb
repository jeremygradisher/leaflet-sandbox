<%- model_class = Area -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => model_class.model_name.human.titleize %></h1>
</div>


<div class="form-group">
    <div class="col-lg-2"></div>
    <div class="col-lg-10">
      <p style="font-style: italic;">*This Name field currently needs to start with a letter and have no spaces or the map will break 
      and you will see a blank space.</p>
    </div>
</div>


<%= render :partial => 'form' %>

